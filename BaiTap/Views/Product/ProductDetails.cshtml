@{
    ViewBag.Title = "Product Details";
}

<h2>Product Details for @ViewBag.ProductName</h2>

@if (!string.IsNullOrEmpty(ViewBag.ImageUrl))
{
    <img src="@ViewBag.ImageUrl" alt="Product Image" />
}
else
{
    <p>Không tìm thấy hình ảnh cho sản phẩm này.</p>
}
